package proto

import "github.com/mauricelam/genny/generic"
import	p "github.com/golang/protobuf/proto"

type T generic.Type

func (self *T) Marshal(t T) ([]byte, error) {
	return p.Marshal(&t)
}

func (self *T) Unmarshal(bytes []byte) {
	p.Unmarshal(bytes, self)
}
